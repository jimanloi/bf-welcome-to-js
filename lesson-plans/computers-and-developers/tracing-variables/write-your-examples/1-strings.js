'use strict';

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare variable myName
// initialise myName to the value 'Manloi Jim'

let myName = 'Manloi Jim';

// read myName:

console.log(myName);

// Reassign value to myName:

myName = 'Peter Smith';

// read myName:

console.log(myName);
